# CaddieOS Build 59

Baseline: Build 58

Purpose: rebuild the native Android APK with the Course-tab runtime helper repair applied before compilation, so bottom navigation remains inside CaddieOS instead of exposing the underlying download/browser screen after a tab-triggered app crash.

No visual redesign or sizing changes in this build.

Build 59 clean workflow trigger: 2.
