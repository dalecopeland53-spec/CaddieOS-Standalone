name: Native Android Standalone APK

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout CaddieOS
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20.20.2

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Create clean native React Native shell
        run: |
          npx --yes @react-native-community/cli@latest init CaddieOSNative --version 0.83.2 --skip-git-init --package-name com.dalecopeland.caddieos

      - name: Install CaddieOS app code
        run: |
          cp App.js CaddieOSNative/App.js

      - name: Build standalone release APK
        working-directory: CaddieOSNative/android
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease --no-daemon

      - name: Collect standalone APK
        run: |
          mkdir -p output
          cp CaddieOSNative/android/app/build/outputs/apk/release/app-release.apk output/CaddieOS-Standalone.apk

      - name: Upload standalone CaddieOS APK
        uses: actions/upload-artifact@v4
        with:
          name: CaddieOS-Standalone-Android
          path: output/CaddieOS-Standalone.apk
          if-no-files-found: error
